#DYCARGO

This repository contains both the **client** (Next.js) and **server** (NestJS) codebase for the Ex-Pay shipment tracking and cross-border logistics platform.

The project is structured as a monorepo for easier development while allowing **separate deployments**:

- **Client → Vercel**
- **Server → Render**

---
